; cPanel shared hosting (CGI/FPM) PHP limits for big installer uploads.
; If your host ignores .user.ini, set the same values in
; cPanel -> MultiPHP INI Editor.
upload_max_filesize = 1500M
post_max_size = 1500M
max_execution_time = 600
memory_limit = 256M
file_uploads = On
